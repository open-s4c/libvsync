set(FILE_NAME cachedq_stress.c)
set(TEST_NAME "test_stress_cachedq")
add_executable(${TEST_NAME} ${FILE_NAME})
target_link_libraries(${TEST_NAME} vsync pthread)
v_add_bin_test(NAME ${TEST_NAME} COMMAND ${TEST_NAME})
